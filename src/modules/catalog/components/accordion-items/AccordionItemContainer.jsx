import React from 'react';

const AccordionItemContainer = ({children}) => {
    return <div className='bg-gray-extra-light rounded-[6px] p-[16px] space-y-[16px] mb-[16px]'>{children}</div>
};

export default AccordionItemContainer;